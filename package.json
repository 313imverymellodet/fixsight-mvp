{
  "name": "fixsight-mvp",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "AI-assisted visual repair triage, technician verification, before/after completion checks, and customer closeout for mechanics.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.mjs",
    "dev": "node --watch server.mjs",
    "check": "node --check server.mjs && node --check public/app.js && node --check public/job-mode.js && node --check tests/smoke.test.mjs && node --check scripts/browser-preview.mjs",
    "test": "node --test tests/*.test.mjs",
    "validate": "npm run check && npm test",
    "preview": "node scripts/browser-preview.mjs"
  }
}
